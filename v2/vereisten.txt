# pip install -r vereisten.txt
#
# Ondergrenzen, geen vastgepinde versies: dit draait op een gewone PC en niet
# in een container, dus een nieuwere patchversie is hier prima.
#
# Twee dingen die je moet weten voor je gaat upgraden:
#
#   opencv-python 5 heeft cv2.CascadeClassifier VERWIJDERD. Geen enkel bestand
#   hier gebruikt het nog, maar oude voorbeelden op internet wel.
#
#   rapidocr-onnxruntime zet zijn draden niet zelf vast en pakt standaard alle
#   kernen per sessie, drie sessies per proces. Zie knip._een_draad_per_sessie:
#   zonder die ingreep is meer werkers TRAGER in plaats van sneller.

opencv-python>=5.0.0
numpy>=2.0
rapidocr-onnxruntime>=1.2.3
onnxruntime>=1.17
requests>=2.31

# Wil je `py run.py --gpu`, dan moet onnxruntime hierboven WEG en hier iets
# anders voor in de plaats:
#
#     pip uninstall onnxruntime
#     pip install onnxruntime-directml
#
# Die twee pakketten leveren allebei de module `onnxruntime` en kunnen niet
# naast elkaar staan; daarom kan dit geen regel in deze lijst zijn.
#
# Het is DirectML en niet CUDA omdat CUDA een apart te installeren toolkit
# vraagt (en er voor Python 3.14 geen wheels voor zijn), terwijl DirectML met
# elke DirectX 12-kaart praat - NVIDIA, AMD en de ingebouwde Intel.
#
# Gemeten: de kaart geeft over alle 225 uitsnedes exact dezelfde tekst als de
# processor, en is anderhalf keer sneller met een vijfde van de kernen. Zie
# LEESMIJ en `py meten/ijkgpu.py`.
#
# onnxruntime-directml loopt achter (1.24 tegen 1.30), dus dit is ook nagemeten:
# zonder --gpu is 1.24.4 even snel als 1.30.0 (afwisselend gemeten, 2,12/2,12 en
# 2,04/2,09 seconde per foto) en leest hij letterlijk dezelfde tekst. Het kost
# dus niets om over te stappen, ook niet als je --gpu een keer niet gebruikt.
